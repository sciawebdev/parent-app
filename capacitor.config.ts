import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saratchandra.scaparentapp',
  appName: 'SCA Parent App',
  webDir: 'dist'
};

export default config;
